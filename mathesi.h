#ifndef MATHESI
#define MATHESI

bool isPrime(unsigned number);
bool isPalindrome(unsigned long long number,unsigned char base = 10);
std::pair<int,int> euclidianDivision(int dividend, int divisor);
unsigned gcd(unsigned a, unsigned b);
#endif // MATHESI

